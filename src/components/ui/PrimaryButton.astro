---
const { class: className } = Astro.props;
---

<button
  class:list={[
    "button-1 py-2 px-5 text-sm focus:outline-none bg-transparent rounded border border-primary-300 text-primary-300 hover:bg-primary-100 focus:z-10 focus:ring-4 focus:ring-gray-200 uppercase",
    className,
  ]}
>
  <slot />
</button>
